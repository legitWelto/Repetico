{
  "name": "repetico",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npm run build && npx cap copy",
    "cap:open:android": "npx cap open android",
    "generate:splash": "node scripts/generate-splash.cjs",
    "cap:assets": "npm run generate:splash && npm run generate:icons && npx @capacitor/assets generate --android",
    "generate:icons": "node scripts/generate-icons.cjs",
    "test": "vitest run",
    "android:deploy": "npm run test && npm run build && npm run generate:icons && npx cap sync android && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.0.0",
    "@vitest/coverage-v8": "^4.1.5",
    "jsdom": "^29.0.2",
    "sharp": "^0.34.5",
    "vite": "^5.2.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@jofr/capacitor-media-session": "^4.0.0",
    "idb": "^8.0.0"
  }
}